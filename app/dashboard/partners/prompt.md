1. implement a delete button on each row in `components\TablePartner.tsx`
2. show dialog confirmation before deletion using `shadcn/ui`
3. you can use `modules\partner\service.ts` or `app\api\partner\[id]\route.ts` for the deletion logic
4. show some feed back for the deletion status (error/success)
